package textoutput;

import java.io.IOException;

public class TextInput {
	void createFile() {
		try {

			try {
				if (!file.exists()) {
					file.createNewFile();
					System.out.println("새 파일 생성함");
				}
			} catch (IOException e) {
				// TODO: handle exception
				e.printStackTrace();
			}
	}
}
